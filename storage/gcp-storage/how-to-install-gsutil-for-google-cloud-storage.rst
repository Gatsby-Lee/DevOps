How to install gsutil for Google Cloud Storage
==============================================

* python2.7 ( python3 is not supported. )
* it will authentigate through OAuth

.. code-block:: bash

  $ pip install gsutil
  $ gsutil config
